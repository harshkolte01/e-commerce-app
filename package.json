{
  "name": "e-comm-backend",
  "version": "1.0.0",
  "description": "E-comme backend API for RENDER",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC"
}